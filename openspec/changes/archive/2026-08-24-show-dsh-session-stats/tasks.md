# show-dsh-session-stats tasks

- [x] 1. Rust：history 尾页解析 `sessionStats` + `cacheWriteTokens`
- [x] 2. Rust：`session/projection` 映射 tokenUsage / sessionStats
- [x] 3. 前端：`ThreadTokenUsage.sessionStats` + history extract + reducer 补丁
- [x] 4. 前端：composer 红框 `DshSessionStatsLine`
- [x] 5. 单测覆盖 format / history / mux / composer 渲染
